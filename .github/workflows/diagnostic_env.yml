# Disabled — referenced script (diagnostic_env.py) doesn't exist
name: Diagnostic Environnement Python

on:
  workflow_dispatch:

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow disabled — missing diagnostic_env.py"
