# Disabled — referenced scripts (orchestrate_all.ps1, healthcheck_orchestrator.py) don't exist
name: Orchestrator Healthcheck

on:
  workflow_dispatch:

jobs:
  healthcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow disabled — missing orchestration scripts"
