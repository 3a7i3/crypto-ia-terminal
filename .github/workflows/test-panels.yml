# Disabled — referenced scripts (test_panels_with_report.py) don't exist
name: Test Dashboards and Panels

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow disabled — missing panel test scripts"
