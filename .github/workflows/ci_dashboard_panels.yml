# Disabled — referenced script (panel_ci_report.py) doesn't exist
name: CI Dashboard Panels

on:
  workflow_dispatch:

jobs:
  test-panels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow disabled — missing panel_ci_report.py"
