# Reprise — Lundi 2026-06-09 (J6)

## Contexte rapide

- Service systemd actif depuis le 07/06 (PID 1149698)
- Telegram **enfin configuré** (token + chat_id corrigés le 07/06)
- Burn-in : 0 trades fermés — positions ouvertes perdues au restart
- Processus parasites nettoyés (plus de duplicates)

---

## Vérifications immédiates au démarrage (5 min)

```bash
# 1. Service vivant ?
sudo systemctl status crypto-advisor --no-pager | head -6

# 2. Telegram fonctionne maintenant ?
sudo journalctl -u crypto-advisor -n 30 --no-pager | grep -E "TelegramAlert|404|Envoi"

# 3. Trades fermés depuis vendredi ?
python3 -c "
import json
path = 'databases/paper_trades.jsonl'
opens, closes = 0, 0
with open(path) as f:
    for line in f:
        d = json.loads(line.strip())
        if d.get('event') == 'OPEN': opens += 1
        elif d.get('event') == 'CLOSE': closes += 1
print(f'OPEN: {opens}  CLOSE: {closes}')
"

# 4. Bot tokens dans systemd ?
sudo grep "BOT_TOKEN\|CHAT_ID" /etc/systemd/system/crypto-advisor.service
```

---

## Ordre de travail semaine (J6–J12)

### J6–J7 (lun-mar, 9-10 juin) — Observation + Dettes techniques

> Si Telegram fonctionne et service stable → dettes techniques en priorité

| Tâche | Durée | Fichier |
|---|---|---|
| Fix bug restore positions (CRITIQUE — TP/SL bloqué) | 2h | `paper_trading/mexc_simulator.py` |
| Vérifier statut paper_runner (PID 946985) | 15 min | VPS |
| Nettoyer spam Telegram REJETE | 30 min | `paper_trading/mexc_simulator.py` |
| Nettoyer runtime_config.json GATE_MIN_SCORE_OVERRIDE | 15 min | `databases/runtime_config.json` |
| Brancher PaperTradeRecorder dans MexcSimulator | 1h | `paper_trading/mexc_simulator.py` |

### J8 (mer, 11 juin) — Bilan J7

Compter les trades fermés :

```
Si N ≥ 15 → démarrer analyse intermédiaire burn-in
Si N < 15  → continuer observation passive jusqu'à J14
```

### J9–J12 (jeu 12 → dim 15 juin)

**Si N ≥ 15 :**
- Rapport burn-in intermédiaire : PnL/régime, WR, expectancy
- Investiguer gate discrepancy (score 59 passe malgré seuil 60)
- Ajuster capital si < $20

**Si N < 15 (probable) :**
- Observation passive — aucun code
- Préparer analyse pour J14 (critère de passage semaine 3)

---

## Bug prioritaire à fixer : MexcSimulator restore

**Symptôme :** Positions ouvertes avant restart sont perdues → TP/SL ne se déclenche jamais → burn-in bloqué à 0 fermés.

**Fix à implémenter :**
Dans `MexcSimulator.start()`, après `self._capital = capital`, ajouter restauration depuis `paper_trades.jsonl` :
- Lire les OPEN sans CLOSE correspondant
- Reconstruire `MexcPosition` avec tp=entry×1.04, sl=entry×0.98 (défauts)
- Réduire `self._capital` du montant des positions restaurées

**Fichiers concernés :** `paper_trading/mexc_simulator.py`, `paper_trading/recorder.py`

---

## Ce qui NE DOIT PAS être touché

- Paramètres ALPHA_DISCOVERY_100 gelés
- Architecture principale (ExecutionRouter, etc.) → post 100 trades fermés
- Levier dynamique — interdit avant validation complète

---

## Liens

- [[../02_Execution/Session_2026-06-07]]
- [[../01_Roadmap/ROADMAP_2SEMAINES_2026-06-04]]
- [[../06_Dashboard/dashboard]]
