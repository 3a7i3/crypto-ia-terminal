# Reprise — 2026-06-05

## État de départ

- MexcSimulator actif sur VPS ($100, 6 positions ouvertes depuis hier 02:30)
- Burn-in en cours — 0 trades fermés, observation passive
- Watchdog systemd déployé
- Gouvernance 63/63 verts

## Priorité immédiate (avant tout)

1. Vérifier que le VPS tourne toujours
2. Regarder le Telegram @QuantCrpto_bot — des positions ont-elles été fermées (TP/SL) ?

```bash
grep "\[SIM\]" logs/advisor.log | grep "CLOSE\|FERME\|TP\|SL\|FILL.*exit" | tail -10
```

## Ordre de travail suggéré

1. **Correction spam Telegram** (30 min) — priorité UX
2. **Self-heal exchange reconnect** (2h) — dette P2 reportée
3. **Nettoyer runtime_config.json** (15 min) — GATE_MIN_SCORE_OVERRIDE=0 à 0 ou à supprimer
4. **PaperTradeRecorder dans MexcSimulator** (1h) — persistance trades

→ Voir [[../01_Roadmap/ROADMAP_2SEMAINES_2026-06-04]] pour le plan complet

## Commandes de reprise VPS

```bash
# État général
ps aux | grep advisor_loop | grep -v grep
systemctl status watchdog_crypto | head -5

# Positions en cours
grep -E "FILL|CLOSE|FERME" logs/advisor.log | grep "$(date +%Y-%m-%d)" | tail -20

# Capital simulateur
grep "Capital" logs/advisor.log | grep "\[SIM\]" | tail -5
```

## Ce qui NE DOIT PAS être touché

- Paramètres ALPHA_DISCOVERY_100 gelés : GATE_MIN_SCORE_OVERRIDE, PB_MIN_POSITION_USD
- Architecture principale : attendre 100 trades fermés pour ExecutionRouter refactor
